import $ from 'jquery';

export const $terminalWindow = $('div#window');
export const $terminalOutput = $('div#output');
export const $directoryLinks = $('div#output a[data-directory]');
export const $terminalTitle = $('span#current-directory');
export const $commandInput = $('input#command');
